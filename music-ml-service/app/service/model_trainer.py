class ModelTrainerService:
    def train(user_track_weights: list)
    def load_training_data()
    def save_model(model, path: str)
    def load_model(path: str)
    def hot_reload_model(path: str)
    def get_active_users(hours: int)